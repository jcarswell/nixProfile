DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
pushd $DIR/../

echo "Uno Mommento while we do a sync"

git add *
[ -z ${1} ] && git commit -a -m "${1}" || git commit -a -m "Someone made a change"
git push

# vim: ts=4:sw=4:syntax=sh
