pushd ${JPRO} > /dev/null

echo "Uno Mommento while we do a sync"

git add *
if [[ "$#" == "1" ]]; then
    git commit -a -m $@
else
    git commit -a -m "Someone made a change"
fi
git push
git pull

popd > /dev/null
# vim: ts=4:sw=4:syntax=sh
